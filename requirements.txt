runpod==1.6.0
# Note: torch and torchaudio are installed in Dockerfile with CUDA 12.4 support
# Note: flash-attn 2.8.3 is installed in start.sh via official wheel (Python 3.11 + PyTorch 2.5.1 + CUDA 12.4)
numpy<2.0.0
scipy
transformers>=4.40.0,<5.0.0
accelerate
huggingface_hub
soundfile
pydub
qwen-tts>=0.0.5
