runpod==1.6.0
# Note: torch and torchaudio are installed in Dockerfile with CUDA support
# Note: flash-attn installs via wheel in Dockerfile
numpy<2.0.0
scipy
transformers>=4.40.0,<5.0.0
accelerate
huggingface_hub
soundfile
pydub

cryptography
